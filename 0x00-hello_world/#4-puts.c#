/**
 * main - Entry point
 *
 * printing a sentence with puts
 * puts writes the chatacters to specified standard output
 *
 * the sentence should end with new line
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
	char my_char[] = "\"Programming is like building a multilingual puzzle";
	       
		puts(my_char);
	return (0);
}

